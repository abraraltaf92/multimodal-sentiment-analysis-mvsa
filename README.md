# multimodal-sentiment-analysis-mvsa
A multimodal sentiment analysis system that combines BERT (text) and CNN (image) features on the MVSA dataset, comparing text-only, image-only, and hybrid models.
